import 'package:flutter/material.dart';

class Agend extends StatelessWidget {
  const Agend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Agenda"),
    );
  }
}
